# 기존 properties를 yml파일로 변경
spring:

  # Spring dataSource setting
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://cocofarm.caqo8iirn0pz.ap-northeast-2.rds.amazonaws.com:3306/cocofarm
    username: jeongyong95
    password: vmfhgod11

    # Connection Pool
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 10

  # JPA setting
  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true

  # Thymeleaf setting
  thymeleaf:
    prefix: classpath:templates/
    suffix: .html
    cache: false
    check-template-location: true

# 설정없이도 기본적인 작동은 합니다.
# 개발모드로 수정하면서 내용 확인하고 싶다면
# 'spring.thymeleaf.cache'를 false로 선언하시면 되고
# 운영 모드에서는 true로 해야 합니다.
